# .gitattributes for {{PROJECT}} (created by Astoria).
#
# New projects normalise text to LF in the repository and check out native to the
# OS, so contributors on Windows/macOS/Linux stay consistent without churn.
* text=auto

# FreeBASIC sources and docs are text.
*.bas  text
*.bi   text
*.frm  text
*.rc   text
*.txt  text
*.md   text

# Build output and binaries.
*.exe  binary
*.dll  binary
*.o    binary
*.obj  binary
*.a    binary
*.lib  binary
