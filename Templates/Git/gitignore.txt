# .gitignore for a FreeBASIC project (created by Astoria).

# Build output
*.exe
*.o
*.obj
*.a
*.lib
*.dll
*.so
*.dylib
*.map

# Debug / temporary
*.log
*.tmp
*.bak
Temp/
temp/
# Astoria writes a scratch copy of an unsaved main file named Temp.bas into the
# project folder (needed there so its relative #includes resolve) -- not source.
Temp.bas

# OS / editor cruft
Thumbs.db
Desktop.ini
.DS_Store
*~
